syntax = "proto3";

message Alert {
  string id = 1;
  string header_text = 2;
  string description_text = 3;
  // Add other fields based on the actual schema
}

message AlertFeed {
  repeated Alert entity = 1;
}
